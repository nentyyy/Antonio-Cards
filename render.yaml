services:
  - type: web
    name: bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python -m app.main_user
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
